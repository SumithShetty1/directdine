function Recommend({ selectedLocation }) {
    return (
      <main>
        <section className="recommend">
          <article>
            <h1>Highly recommended restaurants in {selectedLocation}</h1>
          </article>
        </section>
      </main>
    );
  }

export default Recommend;
